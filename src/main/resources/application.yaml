#services:
#  backend:
#    deploy:
#      resources:
#        limits:
#          memory: 1G
file:
  client-path: /file
  storage-location: /home/dorndana/Pictures/random/upload/
  base-uri: http://localhost:${file.client-path}
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
  application:
    name: ite-ecommerce

server:
  port: 9999



